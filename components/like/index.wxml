<!--components/like/index.wxml-->
<view bind:tap="onLike" class="container">
<image class="like" src="{{like ? likeImg: dislikeImg}}" ></image>
<text class="like-num">{{like?count:count}}</text>
</view>
